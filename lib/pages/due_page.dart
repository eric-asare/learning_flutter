import 'package:flutter/material.dart';

class DuePage extends StatelessWidget {
  const DuePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        body: Center(child: Text("Verses due today. Memorize now!")));
  }
}
